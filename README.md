Windows 11 app install script you can run once on a fresh machine. 
It uses WinGet (built into Win11) and installs a practical “base kit” (browser, archiver, terminals, dev tools, chat, media, etc.). 
You can edit the list to your taste
